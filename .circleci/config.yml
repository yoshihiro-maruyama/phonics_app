version: 2
 
jobs:
  rspec:
    docker:
      - image: circleci/ruby:2.5.3
    working_directory: ~/phonics_app
    steps:
      - checkout
      - run:
          name: Run rspec
          command: sudo docker-compose run web rspec
 
workflows:
  version: 2
  workflows:
    jobs:
      - rspec